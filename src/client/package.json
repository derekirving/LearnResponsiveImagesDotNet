{
  "name": "client",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "process-images": "./index.ts"
  },
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --compile --outfile process-images"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
